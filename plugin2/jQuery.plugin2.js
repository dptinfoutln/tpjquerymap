(function($)
{
    $.fn.secondplugin=function(/* param */)
    {




        $(function () {
            //todo trouver un moyen pour que la fonction soit appelé sur chaque objet et non pas que sur le premier

            //todo faire que L'élément change au passage de la souris.

                //todo On diminue donc l'opacité (fadeOut).
                //todo  fois l'élément invisible, on le fait réapparaitre (fadeIn).


                //todo trouver un moyen d'appeller la popup après l'animation.

                //todo donner des parametres a votre fonction : vitesse d'apparition et de disparition (pas a cette endroit!!!).
                //todo passer les parametres en objet littéral et utiliser des valeur par defauts (pas a cette endroit!!!).
                //pour garder la retrocompatibilite on utilise un objet littéral et des valeurs par defaut
                //si de nouveau parametres voient le jour dans votre plugin les anciens utilisateurs n'auront pas a changer leur code
                // et utiliseront donc les valeurs par defauts sauf si ils le décident


                //todo afficher dans la popup un parametre passé en argument a l'appel de la fonction.
                //todo afficher dans la popup plus de parametres que ce qui vous sont passés.



                //todo gérer le return différemment du premier plugin.

        });
    };
})(jQuery);
//cette syntaxe permet que $ ai pour valeur jQuery
//C'est une technique pour éviter les conflits avec les autres librairies JavaScript utilisant aussi le $

